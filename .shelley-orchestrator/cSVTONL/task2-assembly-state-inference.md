SYSTEM CONTEXT:
You are a Senior Full-Stack Engineer and Agentic Systems Orchestrator operating in July 2026, performing a surgical refactor of the "Prompt Forge" Assembly Line view and shared inference layer.

CODEBASE FACTS (verified — work against these):
- Single-file app: /opt/prompt-forge/prompt-forge.html. Assembly view markup #view-assembly at line 936. JS for assembly roughly lines 2907-3360.
- ASSEMBLY_ROLES (line 2907): 6 default roles — plan, review_plan, taskout, execute, codereview, final (NOT 7). Per-role system prompts in ASSEMBLY_SYSTEMS (line 2917) plus a 'custom' fallback.
- CONFIRMED BUG — stage editing: asAddStage() at line 3111 hardcodes {role:'custom', modelKey:''}. asRenderStages() at line 3089 renders only a number, a read-only role label, a model <select> (asSetStageModel line 3109), and a delete ✕. There is NO <input> for the stage name and NO <textarea> for instructional/system-prompt text, and NO save button — for custom OR existing stages. The custom stage's system prompt is the fixed ASSEMBLY_SYSTEMS.custom string.
- asRun() at line 3245 runs stages sequentially in a for-loop. Only cancel is asStop() (line 3187) — global STOP via #asStopBtn (line 980). No per-stage pause/edit/resume/re-run. Progress track asRenderProgressTrack (line 3153) is display-only.
- CONFIRMED — no compaction: asBuildPrompt(i, userTask) at line 3234 concatenates the RAW full output of every prior stage ("\n--- PRIOR STAGE j: label ---\n" + o.output). No summarization/truncation/token budgeting. No per-stage context-overflow check (checkCtxOverflow at line 1951 is Forge-view-only).
- CONFIRMED — no penalty params: getParams() (line 1672) and asParams() (line 3145) return only {temperature, maxTokens, numCtx}. ollamaGenerate (line 2836) sends options:{temperature, num_ctx, num_predict} — NO repeat_penalty/presence_penalty/frequency_penalty/top_p/top_k. browserGenerate (line 2873) sends only {messages, temperature, max_tokens, stream}. This is the primary structural cause of the repeating-line output loops on small models.
- CONFIRMED — no MoA routing: asAutoAssign (line 3122) round-robins pool[i % pool.length]; asSetStageModel is manual. No router, no capability-tier matching, no diversity guardrail. (qwen3:0.6b is NOT hardcoded — that was a misreport. The defect is the absence of routing/guardrails.)
- Token telemetry: only Forge view has tok/s (startTokRate line 1917). Assembly has none — only asCharCount.
- Testing: test_tabs.js (56 tests, mocked Ollama on localhost:8765) covers assembly stages/run/zip. Run node test_tabs.js. Keep it green.

OBJECTIVE:
(1) Fix the Assembly Line stage data-binding so every stage (custom AND built-in) is fully editable inline with a persistent save; add drag-to-reorder. (2) Kill the repeating-line output loops by adding real sampling/penalty params with dynamic scaling as context fills. (3) Add Mixture-of-Agents routing + per-stage guardrails. (4) Add context compaction between stages. All without breaking the Ollama backend or existing tests.

SPECIFICATIONS & REQUIREMENTS:
1. Stage editing & save (critical): asStages entries must carry editable fields: a display name (label) and an instructional/system-prompt text. asRenderStages must render, per stage: an <input> for the name, a <textarea> for the instructional text, the existing model <select>, and a SAVE affordance (auto-save on blur + an explicit Save button — both fine, but state MUST persist). Use two-way binding: edits update asStages[i] and localStorage; render reflects stored state. Built-in roles start pre-filled with their ASSEMBLY_SYSTEMS text but become editable (do not destroy the defaults — keep them as reset-to-default). Add a "Reset to default" per stage. Ensure custom stages get a real name + instructions. Add input validation (non-empty name; instructions non-empty) with inline error + block run until valid. The stage role/id can remain for prompt-construction categorization but the label+system text are user-owned.
2. Drag-and-drop reordering: Make the stages list reorderable via native HTML5 drag-and-drop (draggable=".role-stage", dragstart/dragover/drop). Reorder asStages to match the new DOM order. Keep the model assignment and outputs bound to the right stage after move (reindex asStepOutputs/asStages consistently). Provide a keyboard-accessible move-up/move-down fallback for a11y.
3. Per-stage run controls: Add per-stage Pause, Edit-output (edit the prior stage's accumulated output mid-run), Resume/Re-run-from-here, and Skip. asRun must support resuming from stage i and re-running a single stage. Keep the global STOP. Show per-stage status (idle/running/paused/done/error) in the progress track and make stages clickable to open their output/editor.
4. Anti-hallucination sampling params: Extend asParams() and the Forge getParams() and BOTH ollama calls (runOllama line 1729/1738 and ollamaGenerate line 2836) and the WebLLM call (runBrowser line 1814 and browserGenerate line 2873) to send: top_p, top_k, repeat_penalty, repeat_last_n, presence_penalty, frequency_penalty, min_p. Add UI sliders for these in the PARAMS section of BOTH Forge and Assembly views (next to existing temp/maxTokens/numCtx), with sensible defaults (repeat_penalty ~1.1, top_p ~0.9, top_k ~40, presence/frequency ~0.0 baseline). Implement DYNAMIC scaling: as the running token count approaches numCtx (e.g. >70% fill), progressively raise repeat_penalty (cap ~1.3) and presence/frequency (cap ~0.5) and optionally lower top_p — to break repetition loops. Add a client-side repetition-loop DETECTOR in appendOutput (line 1902) and asAppendChunk (line 3217): if the last N lines/tokens repeat beyond a threshold, auto-bump penalties and/or auto-stop with a clear toast. Never silently corrupt files.
5. Mixture-of-Agents (MoA) router: Add a capability tier per model in the pool (e.g. light / medium / heavy) with a UI tag, and a role→tier preference map (plan/architect/codereview → heavy; formatting/final → light/medium). asAutoAssign should assign by tier preference when available (not pure round-robin). Add a guardrail: warn + block run if a heavy role is assigned a light model with no heavy model in the pool (user can override). Allow an optional "router" mode where a small model classifies the task and picks the stage model — optional, keep simple.
6. Context compaction: Replace raw concatenation in asBuildPrompt with a compaction step. Between stages, produce a strict JSON/YAML context payload (PRD summary, decisions, file list, open constraints) — either via a dedicated compact call to a light model or a deterministic extractor if no model is available. Pass the compacted payload (plus only the immediately prior stage's raw output) to the next stage. Add a per-stage token budget and a per-stage context-overflow indicator in the UI. Make compaction toggleable (default ON) with a visible "compacted" badge per handoff.
7. Telemetry: Add per-stage token count + tok/s + cumulative tokens + context-fill % to the assembly view (reuse approxTokens at line 1506). Show in the progress track / per-stage panel.
8. No regressions: Keep Ollama backend fully functional (do not change endpoint shape beyond adding options fields). Keep test_tabs.js green; if a test asserts the old asBuildPrompt string format, update the test to match the new compacted format and document why. Keep element IDs stable or add new ones (don't rename).

GUARDRAILS:
- Output real, complete, drop-in edits to prompt-forge.html (exact function replacements with anchors). No pseudo-code.
- Safeguards: assembly must always output something aligned with user settings; never empty/corrupt zips. Validate stage config before run.
- Do not break Forge-view inference (you're extending shared params — wire both).
- Run node test_tabs.js before and after; report counts. Fix new failures.
- Keep all three backends working: Ollama local (default), WebGPU/WebLLM, and (when Task 4 lands) HF/drag-drop — but Task 4 owns HF/drag-drop; you just must not block them.

DELIVERABLE: Patched functions + before/after test_tabs.js counts + a table of new sampling defaults and the dynamic-scaling thresholds chosen.
