SYSTEM CONTEXT:
You are a Senior AI Systems Architect operating in July 2026, specializing in edge-native inference and sandboxed execution. You are expanding Prompt Forge's model selection and adding a literal execution environment to validate agentic output before delivery.

CODEBASE FACTS (verified):
- Single file: /opt/prompt-forge/prompt-forge.html.
- Backends: setBackend('ollama'|'browser') at line 1684. Ollama: fetchOllamaModels (line 1705) GET {base}/api/tags (base default /ollama), runOllama (line 1729) POST {base}/api/generate. Shared layer: ollamaGenerate (line 2836), browserGenerate (line 2873). WebGPU/WebLLM: loadWebLLM (line 1807) dynamic import('https://esm.run/@mlc-ai/web-llm'); _engines registry line 2739; sharedWebLLM line 2742. BROWSER_MODELS catalog line 2718-2735 (15 WebLLM IDs; qwen3:0.6b NOT present).
- CONFIRMED missing: NO ondrop/ondragover/file input for .gguf/.onnx anywhere. NO HF leaderboard / HF API key input / HF API call (only a help-text mention line 1168). No pyodide/webcontainer/wasm sandbox. The Code Reviewer stage (ASSEMBLY_SYSTEMS.codereview ~line 2960) is text-only ("Do NOT rewrite the code. Only review it.").
- The app is explicitly "no accounts, no API keys, no telemetry" by README — but the user WANTS optional HF API key + URL as an ADDITIONAL option, with Ollama local remaining the default and intact.
- Agent Forge interview already mentions gguf_file as an answer option (line 3485) and a gguf_detail follow-up (line 3502) — these describe the GENERATED package, not in-app model loading. Do not confuse the two.
- Testing: test_webgpu_local_http.js validates #browserModel option IDs against WebLLM prebuiltAppConfig over serve.py (localhost:8765). If you add/changes model IDs, keep them valid against WebLLM prebuilt config or update the test intentionally. Run node test_webgpu_local_http.js.

OBJECTIVE:
(1) Add drag-and-drop loading of local .gguf/.onnx model files into the browser (WebGPU/transformers.js pipeline). (2) Add a live Hugging Face leaderboard integration + optional HF API key/URL for cloud inference — as ADDITIONAL options, Ollama stays default. (3) Add a WASM/Pyodide sandbox that actually runs the generated code against the generated tests; feed stderr back to the Code Reviewer for autonomous remediation. All optional, non-breaking.

SPECIFICATIONS & REQUIREMENTS:
1. GGUF/ONNX drag-and-drop: Add a drop zone (ondrop/ondragover/ondragleave) + a fallback <input type="file" accept=".gguf,.onnx"> using the File System Access API where available. Accept .gguf and .onnx. Load via transformers.js (gguf via transformers.js GGUF support / onnx via the ONNX runtime backend) on WebGPU with a CPU fallback. Register the loaded model as a new selectable entry in BOTH the Forge #browserModel select and the Assembly pool (asRenderBrowserPool line 3046) with a clear "local-file" badge and the filename. Cache the loaded model instance. Show load progress (reuse initProgressCallback pattern). Handle unsupported files with a clear error toast. Memory-manage: allow unload. SECURITY: validate file extension + magic bytes; cap size; isolate in a worker if feasible.
2. Live Hugging Face integration: Add an HF section with (a) a live-fetching dropdown of top open text-generation models from the HF Hub (use the public /api/models?filter=text-generation&sort=... endpoint — no key needed for public list; cache + graceful degradation when offline); (b) an <input type="password"> for an optional HF API key + a custom inference URL field, stored only in localStorage (clearly labeled optional, with a "clear" button); (c) when an HF model + key is chosen, route inference to the HF Inference API (or the user's custom URL) as a THIRD backend alongside Ollama and WebGPU. Add setBackend('hf') path. Default backend stays Ollama. Surface July-2026-leading models as suggested defaults in the dropdown (e.g. leading open coding/reasoning models of mid-2026 — use the live list, mark a few "recommended"). Never log the API key. Add a connectivity test button.
3. WASM/Pyodide TDD sandbox: Integrate a sandboxed code execution layer (Pyodide for Python via CDN WASM; for JS, a Web Worker with importMaps or WebContainers if feasible). After the Executor stage emits FILE blocks and BEFORE the Code Reviewer finalizes, the sandbox: (a) writes the generated tests + implementation into an in-memory FS; (b) runs the test command (matching the project's selected test types from Task 3); (c) captures stdout/stderr + exit code; (d) feeds the literal stderr/stdout back into the Code Reviewer stage prompt as "RUNTIME FEEDBACK" so the reviewer can recommend concrete fixes (and, if you support an auto-fix loop, re-run the Executor with the error appended — max N iterations, default 2, configurable). Show sandbox run status + a collapsible log in the UI. Keep it isolated (no main-thread eval of generated code; use the worker/pyodide sandbox).
4. Autonomous error resolution: Wire stderr → Code Reviewer (and optionally → Executor re-run) as above. Never present a package as "done" if tests fail and the loop is exhausted — surface a clear "tests failing" state with the log.
5. Keep Ollama default & intact: All new backends are additive. Do not change the Ollama request shape (beyond the new options fields Task 2 may add). The app must still work fully offline with Ollama-only.

GUARDRAILS:
- Output real, complete edits to prompt-forge.html. No mock code. CDN-loaded libs (transformers.js, pyodide) must be lazy-loaded only when their feature is used, so the base app stays light and offline-capable.
- Drag-drop + HF are OPTIONAL add-ons; if they fail to load (offline/blocked), the app must degrade gracefully — no broken UI.
- Sandbox must be isolated; never eval generated code on the main thread.
- Run node test_webgpu_local_http.js before/after; if you add WebLLM model IDs, keep them valid or update the test intentionally. Report counts.

DELIVERABLE: Patched HTML/JS + before/after test counts + a short note on which CDN libs are lazy-loaded and the sandbox isolation boundary.
